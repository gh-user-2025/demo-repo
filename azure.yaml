# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: demo-repo-project-management
metadata:
  template: demo-repo-project-management@0.0.1

infra:
  provider: bicep
  path: infra

services:
  api:
    project: .
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
